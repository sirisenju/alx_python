# !/usr/bin/python3

def multiple_returns(sentence):
    newTuple = (length, first)

    if sentence == "":
        length = 0
        first = None
        return newTuple

    length = len(sentence)
    first = sentence[0]
    return newTuple


if __name__ == "__main__":
    multiple_returns()